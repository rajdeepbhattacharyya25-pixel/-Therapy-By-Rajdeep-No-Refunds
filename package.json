{
  "engines": { "node": "18.x" },
  "scripts": {
    "build": "vite build",
    "start": "node server/index.js",
    "heroku-postbuild": "npm run build"
  },
  "dependencies": {
    "@google/generative-ai": "^1.0.0", // install exact version with npm i
    "express": "^4.18.2",
    "body-parser": "^1.20.2"
  }
}
